---
const { title, classname } = Astro.props;
---

<article
  class={`group relative bg-white p-8 rounded-2xl shadow-md aspect-square flex items-center justify-center hover:scale-105 transition-all hover:shadow-xl ${classname}`}
>
  <p class="text-lg font-semibold text-gray-800">{title}</p>

  <span
    class="absolute bottom-4 left-1/2 -translate-x-1/2 opacity-0
    group-hover:opacity-100 duration-100
    bg-gray-800 text-white text-xs px-2 py-1 rounded-md transition-opacity
    pointer-events-none z-10"
  >
    {classname}
  </span>
</article>
